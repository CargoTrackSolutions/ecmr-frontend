import { SealMetadataRolePipe } from './seal-metadata-role.pipe';

describe('SealMetadataRolePipe', () => {
  it('create an instance', () => {
    const pipe = new SealMetadataRolePipe();
    expect(pipe).toBeTruthy();
  });
});
